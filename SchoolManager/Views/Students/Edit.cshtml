@model SchoolManager.Models.ViewModel.StudentFormViewModel
@{
    ViewData["Title"] = "Edit Student";
}

<h2>@ViewData["Title"]</h2>

<div class="card shadow-sm">

    <div class="card-header">
        <h4 class="mb-0">Student Information</h4>
    </div>

    <div class="card-body">
        <form asp-action="Edit">
            <input type="hidden" asp-for="Student.Id" />
            <div class="mb-3">
                <label asp-for="Student.Name" class="control-label"></label>
                <input asp-for="Student.Name" class="form-control" />
            </div>
            <div class="mb-3">
                <label asp-for="Student.Classroom" class="control-label"></label>
                <select asp-for="Student.ClassroomId" asp-items="@(new SelectList(Model.Classrooms, "Id","Name"))" class="form-control"></select>
            </div>
            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-primary">
                    Save Changes
                </button>

                <a asp-action="Index" class="btn btn-secondary">
                    Back
                </a>
            </div>
        </form>
    </div>
</div>
